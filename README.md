# Online-Art-Auctioning-Platform
# ArtX — Frontend (React + TypeScript + Vite + Tailwind)

## Quick start

1. `npm install`
2. `npm run dev`
3. Open `http://localhost:5173`

## Notes
- Mock API is included for local development (`src/lib/mockServer.ts`). Replace `api` client with your backend when available.
- Authentication is a simple mock that stores a token in localStorage.
- Design / requirements doc uploaded by you is included at: /mnt/data/Initial Report.pdf